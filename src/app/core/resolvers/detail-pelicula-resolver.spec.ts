import { DetailPeliculaResolver } from './detail-pelicula-resolver';

describe('DetailPeliculaResolver', () => {
  it('should create an instance', () => {
    expect(new DetailPeliculaResolver()).toBeTruthy();
  });
});
